# Copy to .env (or export in your shell) to enable LIVE LLM agents.
# Without this key, the system runs fully offline using deterministic canned proposals.
ANTHROPIC_API_KEY=

# Optional: override the model used by the proposal agents.
DECARB_AGENT_MODEL=claude-opus-4-8
